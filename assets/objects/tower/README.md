# Tower sprite

`tower_sheet_64.png` contains five 64x64 idle-animation frames in one row.
The tower plays them at 5 FPS while visible. Connected towers use the original
colors; disconnected towers are dimmed in code.

The original generated image is retained at `source/tower_generated.png`.
